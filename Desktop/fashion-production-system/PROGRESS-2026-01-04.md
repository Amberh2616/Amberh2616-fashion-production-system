# 開發進度記錄 - 2026-01-04

**狀態：** 前端頁面串接完成 ✅ | 後端多檔案上傳待實現 ⏳

---

## ✅ 今日完成項目

### 1. 前端頁面完整串接

#### Sidebar 導航更新
**檔案：** `frontend/components/layout/Sidebar.tsx`

新增導航項目：
- ✅ Upload (`/dashboard/upload`) - 上傳 Tech Pack
- ✅ Kanban (`/dashboard/samples/kanban`) - Kanban 看板

移除項目：
- ❌ Tech Pack（整合到 Upload）
- ❌ Email（暫不需要）

**最終導航結構：**
```
Dashboard
Upload ← 新增
Styles
BOM
Samples
Kanban ← 新增
Procurement
---
AI Review
Settings
```

#### Dashboard 快速操作連結
**檔案：** `frontend/app/dashboard/page.tsx`

將所有按鈕從 `<button>` 改為 `<Link>`：
- ✅ Upload Tech Pack → `/dashboard/upload`
- ✅ View Styles → `/dashboard/styles`
- ✅ Sample Requests → `/dashboard/samples`
- ✅ View Kanban → `/dashboard/samples/kanban`

#### Samples 頁面增強
**檔案：** `frontend/app/dashboard/samples/page.tsx`

在 Header 添加：
- ✅ "View Kanban" 按鈕（連結到 Kanban 看板）
- ✅ 保留 "New Request" 按鈕

#### Upload 頁面
**檔案：** `frontend/app/dashboard/upload/page.tsx`

功能：
- ✅ 原生拖曳上傳（無需 react-dropzone 依賴）
- ✅ 多檔案選擇
- ✅ 上傳進度顯示
- ✅ 自動創建 Style + Revision
- ✅ 完成後跳轉到 Revision 編輯頁面

**狀態：** 前端 UI 完成，等待後端 API

---

## 📋 完整工作流程設計

```
┌─────────────────────────────────────────────────────────────────┐
│ Step 1: Dashboard                                               │
├─────────────────────────────────────────────────────────────────┤
│ 用戶點擊：                                                       │
│ - Sidebar "Upload" 或                                           │
│ - Dashboard "Upload Tech Pack"                                  │
│                                                                 │
│ 跳轉到 → /dashboard/upload                                      │
└─────────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────────┐
│ Step 2: Upload 頁面                                             │
├─────────────────────────────────────────────────────────────────┤
│ 用戶操作：                                                       │
│ 1. 拖曳或選擇多個 PDF 檔案                                       │
│    - Tech_Pack.pdf                                              │
│    - BOM.pdf                                                    │
│    - Spec.pdf                                                   │
│    - Construction.pdf                                           │
│                                                                 │
│ 系統自動執行：                                                   │
│ 2. 創建 Style + Revision                                        │
│    POST /api/v2/styles/                                         │
│    POST /api/v2/styles/{id}/create-revision/                   │
│                                                                 │
│ 3. 逐一上傳檔案                                                  │
│    POST /api/v2/upload-and-parse/  ← 待實現                     │
│    - file: PDF                                                  │
│    - revision_id: UUID                                          │
│                                                                 │
│ 4. 顯示進度（每個檔案）                                          │
│    ⏳ 處理中... → ✅ 完成 / ❌ 失敗                              │
└─────────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────────┐
│ Step 3: 自動解析 + 翻譯（後端 Celery Task）                      │
├─────────────────────────────────────────────────────────────────┤
│ auto_parse_and_translate.delay() ← 待實現                        │
│                                                                 │
│ 1. 識別 PDF 類型                                                │
│    classify_pdf_type() → 'bom' | 'spec' | 'construction' | 'techpack' │
│                                                                 │
│ 2. 根據類型解析                                                  │
│    BOM.pdf → parse_bom_pdf()                                    │
│      - extract_tables()                                         │
│      - 識別欄位（Material, Supplier, UOM）                       │
│      - machine_translate(material_name) → material_name_zh      │
│      - 存入 draft_bom_data                                      │
│                                                                 │
│    Spec.pdf → parse_spec_pdf()                                  │
│      - extract_tables()                                         │
│      - 識別尺寸點（Chest, Waist...）                            │
│      - machine_translate(point_name) → point_name_zh            │
│      - 存入 draft_measurement_data                              │
│                                                                 │
│    Construction.pdf → parse_construction_pdf()                  │
│      - extract_text()                                           │
│      - 分段識別工序                                              │
│      - machine_translate(description) → description_zh          │
│      - 存入 draft_construction_data                             │
│                                                                 │
│    Tech_Pack.pdf → parse_techpack_pdf()                         │
│      - 使用現有 DraftBlock 系統                                  │
│      - 解析圖文說明（callouts）                                  │
│      - machine_translate(source_text) → translated_text         │
│      - 存入 DraftBlock                                          │
└─────────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────────┐
│ Step 4: 跳轉到編輯頁面                                           │
├─────────────────────────────────────────────────────────────────┤
│ router.push(`/dashboard/revisions/${revisionId}`)               │
│                                                                 │
│ 用戶可以：                                                       │
│ - Draft Review → 檢查圖文翻譯                                    │
│ - BOM Editor → 驗證物料清單（已含中文）                          │
│ - Spec Editor → 驗證尺寸表（已含中文）                           │
│ - Construction Editor → 驗證工序（已含中文）                     │
│                                                                 │
│ 完成後 → 點擊 Verify                                            │
└─────────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────────┐
│ Step 5: Create Sample Request                                   │
├─────────────────────────────────────────────────────────────────┤
│ 從 Samples 頁面或 Revision 頁面                                  │
│ 點擊 "New Request"                                              │
│                                                                 │
│ 選擇：                                                           │
│ - Revision（Tech Pack）                                         │
│ - Brand Name                                                    │
│ - Request Type（Proto/Fit/Sales/Photo）                         │
│ - Quantity                                                      │
│ - Priority                                                      │
│                                                                 │
│ POST /api/v2/sample-requests/ ✅ 已完成                          │
└─────────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────────┐
│ Step 6: P0-1 自動生成 Run + MWO + Estimate                       │
├─────────────────────────────────────────────────────────────────┤
│ create_with_initial_run() ✅ 已完成                              │
│                                                                 │
│ 自動創建：                                                       │
│ - SampleRun #1（draft）                                         │
│ - RunBOMLine（快照 BOMItem，含 material_name_zh）               │
│ - RunOperation（快照 ConstructionStep，含 description_zh）       │
│ - SampleMWO（draft，含雙語快照）                                 │
│ - SampleCostEstimate（draft）                                   │
└─────────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────────┐
│ Step 7: Kanban 看板管理                                          │
├─────────────────────────────────────────────────────────────────┤
│ 從 Samples 頁面點擊 "View Kanban"                                │
│ 或從 Sidebar 點擊 "Kanban"                                       │
│                                                                 │
│ /dashboard/samples/kanban ✅ 已完成                              │
│                                                                 │
│ 功能：                                                           │
│ - 12 欄 Kanban 看板                                              │
│ - 狀態轉換按鈕                                                   │
│ - 批量操作                                                       │
│ - 告警系統                                                       │
│ - Excel/PDF 匯出                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## ⏳ 待實現：後端多檔案上傳系統

### 需要創建的檔案

#### 1. 上傳 API 端點
**檔案：** `backend/apps/parsing/views.py`

```python
@api_view(['POST'])
@parser_classes([MultiPartParser])
def upload_and_parse(request):
    """
    上傳 PDF + 自動解析翻譯

    POST /api/v2/upload-and-parse/

    Body (multipart/form-data):
    - file: PDF 檔案
    - revision_id: StyleRevision UUID

    Returns:
    {
        "status": "success",
        "file_id": "uuid",
        "file_type": "bom" | "spec" | "construction" | "techpack",
        "task_id": "celery-task-id"
    }
    """
```

**註冊路由：** `backend/apps/parsing/urls.py`

#### 2. PDF 類型識別器
**檔案：** `backend/apps/parsing/utils/pdf_classifier.py`

```python
def classify_pdf_type(pdf):
    """
    自動識別 PDF 類型

    規則：
    - BOM: 有表格 + 包含 "Material", "Supplier" 欄位
    - Spec: 有表格 + 包含 "XXS", "XS", "S", "M" 尺寸欄位
    - Construction: 文字為主 + 包含步驟編號
    - TechPack: 圖文混合 + 有紅線註解

    Returns: 'bom' | 'spec' | 'construction' | 'techpack'
    """
```

#### 3. Celery 自動解析任務
**檔案：** `backend/apps/parsing/tasks/auto_parse.py`

```python
@shared_task(bind=True)
def auto_parse_and_translate(self, file_path: str, revision_id: str):
    """
    自動解析 + 翻譯 PDF

    流程:
    1. 識別 PDF 類型
    2. 調用對應解析器
    3. 自動翻譯
    4. 存入資料庫
    """
```

#### 4. 各類型 PDF 解析器

**BOM Parser:**
```python
def parse_bom_pdf(pdf, revision_id):
    # extract_tables()
    # 識別欄位（Material, Supplier, UOM）
    # machine_translate(material_name)
    # 存入 draft_bom_data
```

**Spec Parser:**
```python
def parse_spec_pdf(pdf, revision_id):
    # extract_tables()
    # 識別尺寸點
    # machine_translate(point_name)
    # 存入 draft_measurement_data
```

**Construction Parser:**
```python
def parse_construction_pdf(pdf, revision_id):
    # extract_text()
    # 分段識別工序
    # machine_translate(description)
    # 存入 draft_construction_data
```

**Tech Pack Parser:**
```python
def parse_techpack_pdf(pdf, revision_id):
    # 複用現有 DraftBlock 系統
    from .parse_page4 import parse_all_pages
    return parse_all_pages(revision_id)
```

---

## 📊 技術架構總結

### 前端架構
```
frontend/
├── app/dashboard/
│   ├── page.tsx ✅ (Dashboard 主頁)
│   ├── upload/
│   │   └── page.tsx ✅ (多檔案上傳頁面)
│   ├── samples/
│   │   ├── page.tsx ✅ (Sample Requests 列表)
│   │   └── kanban/
│   │       └── page.tsx ✅ (Kanban 看板)
│   └── revisions/
│       └── [id]/
│           ├── page.tsx ✅ (Revision 編輯)
│           ├── bom/ ✅
│           ├── construction/ ✅
│           └── costing-phase23/ ✅
└── components/layout/
    └── Sidebar.tsx ✅ (導航欄)
```

### 後端架構
```
backend/
├── apps/parsing/
│   ├── views.py ⏳ (需添加 upload_and_parse)
│   ├── urls.py ⏳ (需註冊路由)
│   ├── tasks/
│   │   └── auto_parse.py ⏳ (新建)
│   └── utils/
│       ├── translate.py ✅ (已完成)
│       └── pdf_classifier.py ⏳ (新建)
├── apps/samples/
│   └── services/
│       └── auto_generation.py ✅ (P0-1 已完成)
└── apps/styles/
    └── models.py ✅ (BOMItem/ConstructionStep 翻譯欄位)
```

### 資料流
```
PDF 檔案
  ↓ upload_and_parse()
Celery Task（異步）
  ↓ classify_pdf_type()
對應解析器
  ↓ machine_translate()
雙語 JSON 資料
  ↓ save()
draft_bom_data / draft_measurement_data / draft_construction_data / DraftBlock
  ↓ Verify
BOMItem / MeasurementPoint / ConstructionStep（雙語）
  ↓ Create Sample Request
SampleRun → RunBOMLine / RunOperation（雙語快照）
  ↓ Generate MWO
SampleMWO（雙語 JSON）
  ↓ Export PDF
MWO PDF（雙語顯示）
```

---

## ✅ 已完成功能清單

### 前端
- [x] Sidebar 導航更新（Upload + Kanban）
- [x] Dashboard 快速操作連結
- [x] Upload 頁面 UI（拖曳 + 進度顯示）
- [x] Samples 頁面 Kanban 按鈕
- [x] 完整頁面串接

### 後端
- [x] DraftBlock 翻譯系統（Tech Pack 圖文）
- [x] machine_translate() 翻譯引擎（GPT-4o-mini）
- [x] BOMItem / ConstructionStep 翻譯欄位
- [x] P0-1 自動生成（Run + MWO + Estimate）
- [x] 翻譯快照（auto_generation.py）
- [x] Kanban 看板（12 欄 + 狀態機）

---

## ⏳ 待實現功能清單

### Week 1: 後端上傳 + 解析基礎
- [ ] upload_and_parse API 端點
- [ ] 註冊路由到 urls.py
- [ ] PDF 類型識別器（pdf_classifier.py）
- [ ] Celery auto_parse_and_translate 任務

### Week 2: 各類型解析器
- [ ] BOM PDF 解析器（表格提取 + 翻譯）
- [ ] Spec PDF 解析器（尺寸表提取 + 翻譯）
- [ ] Construction PDF 解析器（文字提取 + 翻譯）
- [ ] 整合現有 Tech Pack 解析（DraftBlock）

### Week 3: 測試 + 優化
- [ ] 使用真實 PDF 測試（demo 資料夾）
- [ ] 驗證翻譯品質
- [ ] 修正欄位識別錯誤
- [ ] 前端 UI 優化
- [ ] 錯誤處理優化

---

## 🎯 最終目標

**用戶體驗：**
```
拖曳 4 個 PDF → 等待 2-3 分鐘 → 所有內容已解析 + 翻譯完成 ✅

工廠人員可以：
- 在 BOM Editor 看到中文物料名稱
- 在 Spec Editor 看到中文尺寸點
- 在 Construction Editor 看到中文工序
- 在 MWO PDF 看到雙語輸出

完全不需要手動翻譯！🎉
```

---

**下一步：實現後端多檔案上傳 + 自動解析系統**
